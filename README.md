# FFmpeg-converter-GUI

FFmpeg を使って、動画・音声・画像ファイルの変換を簡単に行うための  
Windows 向け GUI ツールです。

コマンドライン操作が必要な FFmpeg を、  
できるだけ手軽に扱えるようにすることを目的としています。

---

## 概要

- FFmpeg を内部で使用したファイル変換 GUI
- Windows 専用

変換元ファイルと変換後ファイルを指定し、  
「変換」ボタンを押すだけで処理が実行されます。

---

## 使い方

1. 「入力ファイル」ボタンから変換元ファイルを選択  
2. 「出力ファイル」ボタンから変換後ファイルの保存先を指定  
3. 「変換」ボタンを押す  

変換形式は、  
**出力ファイル指定時の拡張子**によって自動的に決定されます。

---

## FFmpeg のインストール方法（Windows）

本ソフトを使用するには、FFmpeg がインストールされており、  
`ffmpeg` コマンドに PATH が通っている必要があります。

### winget を使う方法（推奨）

PowerShell を開き、以下を実行してください。

> winget install ffmpeg

インストール後、次のコマンドで動作確認ができます。

> ffmpeg -version

バージョン情報が表示されれば、正しくインストールされています。

---

## 今後の予定

- 変換形式を選択式で指定できる機能の追加  
- FFmpeg コマンドを手動編集できる機能の追加  
- UI / UX の改善  

---

## 開発について

本ソフトは、作者自身の学習目的として実験的に作成していますが、  
実用可能なツールを目指して、今後も機能追加・改善を行っていく予定です。

---

## 作者

- @yanagi_lpc

---

## ライセンスについて

本ソフトは FFmpeg を利用して動作しますが、  
FFmpeg 本体は同梱していません。

FFmpeg のライセンスについては、公式情報をご確認ください。
